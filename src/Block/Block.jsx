import React from 'react';

const defaultCurrencies = ['AMD','RUB','USD','EUR'];

export default function Block({value, currency, onChangeValue, onChangeCurrency}) {
  return (
    <div>
      
    </div>
  )
}
